# frontend

## 一.项目的创建和依赖安装

### *1.1 构建项目
可以通过vite来快速构建一个react的项目：

```bash
npm create vite@latest frontend -- --template react
```

### 1.2 依赖安装和启动

如果你是拉取该项目，可以通过下面命令来安装依赖

```bash
npm install
```

构建完成之后，可以通过以下命令来运行 
```bash
npm run dev
```

